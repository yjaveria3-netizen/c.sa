#include <stdio.h>

int main() {
    int n;
    printf("Enter size of matrix: ");
    scanf("%d", &n);
    
    int matrix[n][n];
    printf("Enter matrix elements:\n");
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            scanf("%d", &matrix[i][j]);
        }
    }
    
    printf("Matrix in braided pattern:\n");
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            if ((i % 6 == 0 && j % 2 == 0) || (i % 6 == 1 && j % 2 == 1) || 
                (i % 6 == 2 && j % 2 == 0) || (i % 6 == 3 && j % 2 == 1) || 
                (i % 6 == 4 && j % 2 == 0) || (i % 6 == 5 && j % 2 == 1)) {
                printf("%d ", matrix[i][j]);
            } else {
                printf("  ");
            }
        }
        printf("\n");
    }
    
    return 0;
}
